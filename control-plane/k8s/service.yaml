apiVersion: v1
kind: Service
metadata:
  name: session-manager
  namespace: browser-streamer
spec:
  type: ClusterIP
  selector:
    app: session-manager
  ports:
    - port: 8080
      targetPort: 8080
