kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Must match nodePort in k8s/local/service.yaml
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
