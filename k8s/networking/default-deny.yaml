# Default deny all ingress AND egress in the namespace.
# Each workload must explicitly allow inbound and outbound traffic.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: browser-streamer
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
