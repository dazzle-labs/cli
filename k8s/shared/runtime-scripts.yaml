apiVersion: v1
kind: ConfigMap
metadata:
  name: runtime-scripts
  namespace: browser-streamer
  labels:
    app: browser-streamer
# Data is populated at deploy time via:
#   kubectl create configmap runtime-scripts \
#     --from-file=prelude.js=runtime/dist/prelude.js \
#     --from-file=renderer.js=runtime/dist/renderer.js \
#     --dry-run=client -o yaml | kubectl apply -f -
data: {}
