apiVersion: v1
kind: Service
metadata:
  name: ingest
  namespace: browser-streamer
spec:
  selector:
    app: ingest
  ports:
    - name: rtmp
      port: 1935
      targetPort: 1935
      protocol: TCP
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
